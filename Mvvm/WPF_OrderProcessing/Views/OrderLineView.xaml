<UserControl x:Class="WPF_OrderProcessing.Views.OrderLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_OrderProcessing.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
<TextBox Text="{Binding ProductName, Mode=TwoWay}"></TextBox>
        <CheckBox IsChecked="{Binding Selected, Mode=TwoWay}" Grid.Column="1"></CheckBox>
    </Grid>
</UserControl>
